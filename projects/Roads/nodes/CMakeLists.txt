
FILE(GLOB_RECURSE NODE_SOURCES src/*.cpp)

TARGET_SOURCES(zeno PRIVATE ${NODE_SOURCES})
TARGET_LINK_LIBRARIES(zeno PUBLIC Roads)

TARGET_INCLUDE_DIRECTORIES(zeno PRIVATE ${NANOVDB_INCLUDE_FILES} include/)

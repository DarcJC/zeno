#pragma once

#if defined(ZENO_LOAD_PATH)
#include "roads/thirdparty/zpp_bits.h"
#else
#include "zpp_bits.h";
#endif

namespace roads {
    void TestFunc();

    class RoadRPCServer {
    public:
    };
}
